# Changelog Hari 1

## 2026-07-07

- Added real Pokemon pulls from PokeAPI with `Promise.all` for `/pokemon/{id}` and `/pokemon-species/{id}`.
- Added rarity rolls with pity, shiny variants, and cached fetch results.
- Added Pokemon type badges, 6 base-stat bars, history chips, and collection tracking.
- Added Kanto gym badge progress, toast notifications, and persistent state via `localStorage`.
- Added a dedicated changelog to separate release notes from the development log in `LOG.md`.
